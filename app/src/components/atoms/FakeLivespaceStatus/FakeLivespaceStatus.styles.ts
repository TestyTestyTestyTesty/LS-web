import { styled } from "@mui/material"

export const FakeLivespaceStatusLink = styled("a", {
	name: "FakeLivespaceStatusLink",
})({
	display: "flex",
	gap: "0.5rem",
	alignItems: "center",
	textDecoration: "none",
})
